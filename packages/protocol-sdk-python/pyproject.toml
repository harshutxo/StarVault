[project]
name = "starvault-protocol-sdk"
version = "0.1.0"
description = "Python SDK for the StarVault Protocol"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27.0"]

[tool.setuptools.packages.find]
where = ["src"]
